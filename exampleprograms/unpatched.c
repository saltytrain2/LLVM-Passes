#include <stdio.h>
int main() {
    
    int* global_ptr = NULL;

    if(global_ptr == NULL){
        //Code that would dereference global_ptr
        return 1;
    }

    if(global_ptr == NULL){
        //Code that would dereference global_ptr
        return 2;
    }

    if(global_ptr == NULL){
        //Code that would dereference global_ptr
        return 3;
    }
    
    if(global_ptr == NULL){
        //Code that would dereference global_ptr
        return 4;
    }

    return 0;
}
    